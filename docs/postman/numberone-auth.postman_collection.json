{
  "info": {
    "name": "NumberOne Auth",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Login por CPF",
      "event": [
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "const body = pm.response.json();",
              "if (body.accessToken) {",
              "  pm.collectionVariables.set('accessToken', body.accessToken);",
              "}"
            ]
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": {
          "raw": "{{baseUrl}}/auth/login",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "auth",
            "login"
          ]
        },
        "body": {
          "mode": "raw",
          "raw": "{\n  \"cpf\": \"12345678909\"\n}"
        }
      }
    },
    {
      "name": "Rota admin protegida",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Authorization",
            "value": "Bearer {{accessToken}}"
          }
        ],
        "url": {
          "raw": "{{baseUrl}}/api/admin/ordens-servico",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "api",
            "admin",
            "ordens-servico"
          ]
        }
      },
      "description": "O cliente envia apenas o Bearer JWT. O API Gateway valida o token e injeta os headers X-Authenticated-* para o backend."
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "https://api-id.execute-api.us-east-1.amazonaws.com"
    },
    {
      "key": "accessToken",
      "value": ""
    }
  ]
}
